import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.voiceassistant',
  appName: 'Voice Assistant UI',
  webDir: 'dist'
};

export default config;
